<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mycompany.webapp.dao.ISurveyRepository">

	<insert id="setQuestInsert" parameterType="SQD">
		<![CDATA[
				INSERT INTO SURVEY_QUESTION(QUESTION_ID, SURVEY_ID, QUESTION_CONTENT, QUESTION_TYPE_CODE)
				values (#{questionId}, #{surveyId}, #{questionContent}, #{questionTypeCode})
		]]>
	</insert>
	
	<select id="selectMaxQuestionId" resultType="int">
	
	<![CDATA[
				SELECT NVL(MAX(QUESTION_ID), 0) AS "questionId" FROM SURVEY_QUESTION
		]]>
	</select>
	
	<select id="selectMaxItemId"  resultType="int">
		<![CDATA[
				SELECT NVL(MAX(ITEM_ID), 0) AS "itemId" FROM SURVEY_ITEM
		]]>
	</select>
	
	<insert id="setItemInsert" parameterType="SQD">
		<![CDATA[	
			INSERT INTO SURVEY_ITEM(QUESTION_ID, ITEM_ID, ITEM_CONTENT, ITEM_SCORE)
			values (#{questionId}, #{itemId}, #{itemContent}, #{itemScore})
		]]>
	</insert>
	
	<insert id="setSurvey" parameterType="SLD" >
		
		<selectKey keyProperty="surveyId" resultType="int" order="BEFORE">
			select SURVEY_LIST_SEQ.nextval from dual	
		</selectKey>
		
		insert into SURVEY_LIST (SURVEY_ID,SURVEY_NAME, SURVEY_CONTENT,
		ANONYMITY_CHECK_CODE,SURVEY_STATR_DATE,SURVEY_CLOSED_DATE,DECIDE_CHECK) 
		values(#{surveyId},#{surveyName},#{surveyContent},#{anonymityCheckCode},
		#{surveyStartDate},#{surveyClosedDate},#{decideCheck})
	</insert>
	
	<update id="setSurveyUpdate" parameterType="SLD">
		update SURVEY_LIST set SURVEY_NAME=#{surveyName}, SURVEY_CONTENT=#{surveyContent},
		ANONYMITY_CHECK_CODE=#{anonymityCheckCode},SURVEY_STATR_DATE=#{surveyStartDate},SURVEY_CLOSED_DATE=#{surveyClosedDate}  where survey_id=#{surveyId}
	</update>
	
	<insert id="setItemUpdate" parameterType="SQD" >
		
		<selectKey keyProperty="itemId" resultType="int" order="BEFORE">
			select item_id_seq.nextval from dual	
		</selectKey>
		
		insert into survey_item (QUESTION_ID,ITEM_ID, ITEM_CONTENT,
		ITEM_SCORE) 
		values(#{questionId},#{itemId},#{itemContent},#{itemScore})
	</insert>
	
	<delete id="setItemDelete" parameterType="SQD">
		delete from survey_item where question_id = ${questionId}
		
	</delete>
	
	<select id="selectQuestion" parameterType="int" resultType="hashmap">
		select sq.survey_id, sq.question_id, sq.question_content, sq.question_type_code, 
		si.item_id, si.item_content, si.item_score 
		from SURVEY_ITEM si, survey_question sq 
		where si.question_id=sq.question_id and survey_id = #{surveyId}	 order by question_id desc
	</select>
	
	<select id="selectSurveyList" resultType="com.mycompany.webapp.dto.SurveyListDTO">

	
		select SURVEY_ID as surveyId, SURVEY_NAME as surveyName, SURVEY_CONTENT surveyContent, ANONYMITY_CHECK_CODE as anonymityCheckCode,
		SURVEY_STATR_DATE as surveyStartDate,  SURVEY_CLOSED_DATE as surveyClosedDate, DECIDE_CHECK as decideCheck
			   from survey_list 
			   order by rownum desc
		
	
	</select>
</mapper>